<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <!-- <meta name="viewport" content="width=device-width, initial-scale=1.0"> -->
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter&family=Staatliches&display=swap" rel="stylesheet">
    <link href="./style.css" rel="stylesheet">
    
    <title>Document</title>
</head>

<body>
    <div class="spacer layer_bottom">
    <div class="container">
        <h1>JOR.AT</h1>
        <form action="/createShort" method="POST" class="my-4 form-inline">
            <p>Enter the URL you want shrinked</p>
            <input required 
                <% if (invalid) { %> 
                    value="<%= longURL %>"
                <% } %>
                type="url" 
                placeholder="http://..." 
                name="fullUrl" id="fullUrl" 
                class="link">
            <% if (invalid) { %>
              <p class="error"><%= shortURL %> is already used, please try something else</p>
            <% } else {%>
              <p>You can also enter a custom alias for the URL</p>
            <% } %>
            <input placeholder="(optional)" name="fullUrl" id="fullUrl" class="link">
            <button class="btn" type="submit">Shrink</button>
        </form>
    </div>   
    </div>
</body>

</html>